# PAPERCUT company server — placeholder

Nothing is built here yet. The company server (accounts, subscription,
`/tts` and `/agent` proxies to OpenAI) is Phase 10 of the build order.
See docs/03-ARCHITECTURE.md §6b and docs/10-PROJECT-TRACKER.md.
