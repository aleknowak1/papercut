# DOC-04 — Change Log

**Status:** Active

Every change to the software or the documentation is recorded here, newest first. Format: ID · date · area · summary · reason · docs touched.

| ID | Date | Area | Change | Reason | Docs updated |
|----|------|------|--------|--------|--------------|
| CL-0035 | 2026-09-02 | App | Mask-editor refinement from Alek's hands-on use: a brush cursor replaces the crosshair. A thin light ring with a thin dark ring inside (readable on the photo and on the red tint) is drawn at the mouse with EXACTLY the brush's diameter on the photo — it scales with zoom (grows/shrinks with the picture, not the screen) and follows [ ] size changes live; when feather > 0 a fainter dashed ring shows the soft edge's real outer extent (two box passes spread ±2× the radius — proven against featherMask in the check). The system cursor is hidden over the canvas and returns while Space-panning. Pure geometry in shared/segmentation/brushCursor.ts; the overlay is DOM-only and never touches the mask or any saved file. Live-measured in the running app: ring 17.0 px at zoom 0.425 for brush 40 (exact), rescales with zoom, cursor 'none'→'grab' on Space. New check: diameters at several zoom levels; feather ring matches the real blur extent; geometry leaves mask bytes untouched. M-2.4 updated. `npm run check`: 71 s, all green, tests/output empty. | Alek's refinement request after verifying Phase 3. | DOC-04, 05, 10 |
| CL-0034 | 2026-09-02 | Verification | Alek verified the rest of Phase 3 by hand on the dev laptop: characters and poses; the mask editor (paint, feather, save, document undo back to the previous cutout, HD cutout); audio import with real MP3 and OGG files. All worked. HEIC's works-path (a real iPhone photo) remains the one outstanding spot-check (§5). Phase 3 stands Complete. | DOC-11 §4 step 5, as CL-0017 and CL-0030. | DOC-04, 10 |
| CL-0033 | 2026-09-02 | App | Phase 3 — audio import (M-2.6), completing the phase's features. MP3/WAV/M4A/OGG by "+ Audio…" button and drag-and-drop (dropped sounds import straight away; dropped pictures still get the what-are-they question); files copied byte-for-byte into assets/audio/; duration read by decoding with Chromium (OfflineAudioContext — the readability proof too); duplicates and unsupported/damaged files refused in plain English; ♪ rows with duration and a Play/Stop button. Checks: main-side round-trip byte-identical + refusals (vitest); audio FIXTURES decode with the right duration through the real Chromium decoders — a code-generated WAV and an M4A made with the Phase 2 WebCodecs AAC encoder + mp4-muxer — riding inside the export check's hidden window so the suite still boots the app once (both pass: 2.000 s / 2.005 s). MP3 and OGG cannot be generated in code; Alek verifies those with real files (nothing downloaded). Live-verified in the running app (real WAV → row → Play/Stop). Manual M-2.6 written — all M-2 sections now exist and every Phase 3 feature is ☑: Phase 3 features complete. | Phase 3 kickoff step 7 / this session's step 3. | DOC-04, 05, 10 |
| CL-0032 | 2026-09-02 | App | Phase 3 — the mask editor (M-2.4, M-2.4b), built to the DOC-10 §1b design exactly. Plain 2D canvas showing the photo with removed areas red-tinted; brush add/erase (A/E) with size ([ ]), whole-mask edge feather, wheel/keyboard zoom, Space/middle-drag pan. Strokes undo LOCALLY (Ctrl+Z/Y inside the editor, patch-based so memory grows with painted area, not photo size; capture-phase keys so document undo is untouched). Save composes original RGB + edited alpha IN THE MAIN PROCESS through our own PNG code (never a canvas, DOC-13 §9.5), writes assets/cutouts/<name>-v2.png (-v3, …) and makes ONE repoint edit — document undo steps back to the previous file, which is kept (OQ-021 tidies later). "Reset to automatic" repoints at the original automatic cutout (remembered in the asset's metadata.automaticFile). "HD cutout" runs the full model through the existing one-at-a-time queue with the takes-about-a-minute warning and replaces the mask. New pure modules (shared/segmentation/mask.ts, main/cutoutVersions.ts) + new IPC (readCutoutPixels, saveCutoutVersion). Checks: known stroke and known feather on a known mask give expected pixels; patches restore bytes exactly; version naming counts up; saved RGB byte-identical; repoint+undo round trip. Live-verified in the running app: paint → feather → local undo/redo → Save → repointed with automaticFile → document Undo → previous file, both files on disk. Manual M-2.4/M-2.4b written. 101 tests green. | Phase 3 kickoff step 6 / this session's step 2. | DOC-04, 05, 10 |
| CL-0031 | 2026-09-02 | App | Phase 3 — characters with poses (M-2.5). New document edits (rename/remove character; add/rename/reorder/remove pose) with structural sharing; pose records now validated on load (a damaged pose names its field plainly). New Characters panel beside Assets: create character, inline rename (Enter/Escape), add pose from any imported cutout (picker shows the source photo's name, "(HD)" marked), rename/reorder (↑↓)/remove poses, delete character — every operation one undo step through the same applyEdit path; cutouts always stay in the project. Live-verified by driving the real app (create, add 2 poses, rename, reorder, delete, undo restores, saved document matches). New characters check: round-trip save/reopen, one-undo-step per operation, clamped reorder, plain-language validation. Manual M-2.5 written. All checks green. | Phase 3 kickoff step 5. | DOC-04, 05, 10 |
| CL-0030 | 2026-09-02 | Verification / Docs | Alek manually verified the Fable half of Phase 3 on the dev laptop: automatic cutout on a real photo of a person — laptop stayed responsive throughout, cutout completed quickly, result had no rough edges. HEIC import not yet tried (its §5 row stays). Alek's decision: Phase 3 continues on Fable rather than switching to Opus, for consistency; DOC-10 §2 updated, §1b hand-off notes stay as the work description. `npm run check` confirmed green before new work (85 tests + licenses + build scan + segmentation 38.5 s/photo + export; ≈85 s total). | DOC-11 §4 step 5 (Alek verifies before work continues), as CL-0017 did for Phase 1. | DOC-04, 10 |
| CL-0029 | 2026-09-02 | App | Phase 3 step 4 — HEIC (iPhone photos) via Windows' own decoder, nothing bundled (OQ-016, DOC-08 A13). A HEIC import spawns one separate, Microsoft-signed process — WINDOWS PowerShell 5.1 (powershell.exe, ships with Windows; deliberately not PowerShell 7, where the Windows imaging projection is unreliable) — which decodes through the Windows Imaging Component (picking up Microsoft's HEIF Image Extension when installed) and writes a PNG; the PNG enters the normal import path (stored as .png, original file name kept in the record; the only exception to copied-unchanged, per OQ-016). If the extension is missing, the friendly "install the free extension or export as JPG" message shows and nothing is added; a damaged file gets its own message. File dialog accepts .heic/.heif. This laptop HAS the extension (Microsoft.HEIFImageExtension 1.2.48.0; check with: powershell Get-AppxPackage *HEIFImageExtension*), so Alek can verify the works-path with a real iPhone photo. Checks: extension detection, error classification (WIC 0x88982F50 → friendly path), both refusal messages via an injected fake converter, converted-PNG round trip, and the real powershell pipeline exercised end to end by feeding it a PNG renamed .heic (WIC sniffs content) — no HEIC downloaded, per the plan. All checks green. | Phase 3 plan step 4, with Alek's adjustment: PowerShell 5.1, not 7. | DOC-04, 10 |
| CL-0028 | 2026-09-02 | App | Phase 3 step 3 — image import and the Assets panel. JPG/PNG/WebP by button and drag-and-drop (drop asks what the pictures are: backgrounds get no cutout, characters/props queue an automatic one). Files are copied byte-for-byte into assets/images/ (verified by check); duplicates refused by content hash and unreadable files refused after a real decode attempt, both in plain English; thumbnails to cache/thumbnails/. The opened-project placeholder became a project view with the Assets panel (dense list: thumbnail, name, kind, size, live cutout status with per-row Cancel), Undo/Redo buttons and Ctrl+Z / Ctrl+Y / Ctrl+Shift+Z — every import is one undo step through the history engine, and the document saves shortly after each change. New IPC: choose/read/import image, enqueue/cancel cutout, status push channel; drag-drop paths via Electron's webUtils. CSP now allows blob: images (thumbnails) and blob: media (audio, step 7). Live-verified by driving the real dev app end to end: import → real 30 s cutout in the utility process with live status in the panel → cutout row with transparent background. New import check (round-trip byte-identical, duplicate/unsupported/unreadable refusals, save/reopen + undo with assets). Manual sections M-2.1, M-2.2, M-2.3 and M-9.2 written. All checks green (~80 s). | Phase 3 plan step 3. | DOC-04, 05, 10 |
| CL-0027 | 2026-09-02 | Decision + App | Alek's OQ-020 decision recorded as ADR-017 (fp32 on CPU; targets revised to <15 s reference / ≤45 s minimum-spec, background queue always; native-1024 inference; 4096 working-copy cap; one cutout at a time; DirectML deferred to OQ-022; HD delivery question OQ-023; OQ-020 closed). Engineering round in DOC-13 §10: no pre-built quantized variant exists in either pinned repo, so fp32 alone ships (no Python toolchain); session tuning measured — memory arena OFF is ~35–45% faster AND drops held memory from 2.2–2.8 GB to ~105 MB after each job (the ~4–4.7 GB peak during compute is the model's real working memory, untunable); model resident while queue busy, whole worker process ended when idle; HD full fp32 fetched (973 MB, hash-verified) and measured: 36–44 s/photo here. Phase 3 step 2 built: segmentation worker (Electron utility process, out/main/segmentation-worker.js), job queue with status/cancellation (queued job dequeued; running job stopped by ending the worker), LocalBiRefNetProvider implementing SegmentationProvider, hand-written PNG encode/decode (original pixels + alpha, no canvas), pure pixel modules under app/shared/segmentation. New checks in npm run check: Build scan (production build; no dev/fixture code shipped; worker built; onnxruntime-node external; models pinned+present+configured) and Segmentation (real worker, real model, fixture mask coverage, RGB byte-identical to source, worker memory back under 600 MB; time reported not judged; missing model fails naming the fetch script); opt-in npm run check:segmentation:hd run once (53.9 s/photo). Old unit tests patched to clean tests/output (housekeeping rule); npm run check now ~80 s (was ~25 s) — the real inference is most of it. All checks green. | Alek's decision message: fp32 baseline, measure quantized/none exists, memory work, queue design, resume step 2. | DOC-01, 02 (ADR-017), 03, 04, 07, 10, 13 |
| CL-0026 | 2026-09-02 | Decision gate (round 2) | OQ-020 second measurement round, as directed by Alek. Confirmed the probe always ran the model at its native 1024×1024 (the ONNX input is fixed; the 27.7-min fp16 figure was a fair baseline). Fetched BiRefNet_lite fp32 (224 MB, Alek-approved, hash-verified, pinned in the manifest). Probe extended: per-stage timings (shrink / model / mask-upscale), --model/--ep/--save switches, DirectML with honest CPU fallback, and a hand-written zlib PNG writer so cutouts are original-pixels-plus-alpha with no canvas. Results (DOC-13 §9): fp32 CPU 38.1 s plain Node / median 33.2 s in the Electron utility process, mask quality identical to fp16 to four decimals, upscaled 3000×4000 mask visually clean, cutout PNG byte-verified (bg alpha 0, figure 255, RGB untouched); DirectML compiles but dies out-of-memory on this 8 GB minimum-spec machine with both models — accelerator at best, never the baseline. Honest extrapolation: no CPU path reaches 3 s (reference machine ≈12–25 s fp32). Recorded Alek's two binding pipeline rules (native-1024 inference; 4096-long-edge working-copy cap — analysis says 4096 is right: 2× zoom at 1080p needs 3840, so 3072 and 2048 fail; cap saves 4× memory/time on 48 MP photos at zero mask-quality cost). Paths 4 and 5 ruled out by Alek. No ADR changed; nothing built past the gate. | Alek's DOC-13 decision request: measure paths 1 and 3 before choosing. | DOC-04, 07, 08, 10, 13 |
| CL-0025 | 2026-09-02 | App / Decision gate | Phase 3 OQ-020 gate run — prove or stop. Added onnxruntime-node 1.29.0 (exact; MIT; postinstall downloads nothing on Windows; every binary including the native module is Microsoft-signed). New scripts/fetch-models.mjs + pinned scripts/model-manifest.json (exact repo, revision, file, size, SHA-256; refuses a mismatched hash); BiRefNet_lite fp16 (114.5 MB) fetched into git-ignored models/ and hash-verified. New reusable probe scripts/oq020/ (plain Node + Electron 44 utility process): module loads in both, session builds over the real model, zero Smart App Control / code-integrity events (checked as in CL-0022) — the Phase 1 style block did not occur; only the known @electron-internal/extract-zip block appears in today's log, from before this session. Real inference on a code-generated 3000×4000 figure-on-busy-background fixture: mask nearly perfect (0.998 figure / 0.0005 background) BUT ≈27.7 minutes per photo on CPU, peak 3.9–4.7 GB, ~550× off the <3 s target — fp16 models have no native CPU support in ONNX Runtime (Microsoft recommends fp32 on CPU). STOPPED per the approved plan: no cutout feature built; candidate paths (fp32, int8, DirectML, WASM, lighter model) in DOC-13 §6 for Alek. All existing checks stay green (licenses: 386 packages incl. onnxruntime-node). | Phase 3 plan step 1 (the gate comes first; prove or stop). | DOC-00, 04, 07 (OQ-020 evidence, new OQ-021), 08 (A7, A8), 10, 13 (new) |
| CL-0024 | 2026-09-02 | Repo / Docs | Phase 3 session start housekeeping: pushed the pending docs commit (b11791a); deleted the untracked _to_delete/ folder (stale git lock files); emptied tests/output/ (147 leftover run folders from earlier check runs). New standing rule in CLAUDE.md (and DOC-11 Appendix A): every check cleans up its own output on success — a green `npm run check` leaves tests/output/ empty; a failed check may keep its output for diagnosis and must say where. Phase 3 kickoff prompt recorded as DOC-11 Appendix D. | Phase 3 kickoff housekeeping, directed by Alek. | CLAUDE.md, DOC-04, 10, 11 |
| CL-0023 | 2026-09-02 | Docs | DOC-11 Appendix C added: the Phase 2 kickoff prompt as used (plan-first, prove-or-stop on ADR-013). Cowork project doc copies synced from the repo (DOC-00 to DOC-12) per DOC-11 §7. | Keep kickoff prompts on record and the two doc sets aligned after Phase 2. | DOC-04, 11 |
| CL-0022 | 2026-09-02 | App | Blank-window root cause found by instrumentation, not guessing, and fixed. New permanent start-up log (logs/startup.log in the user-data folder) records every step — process start, dev server reachable, window created, page loads, preload, React mount, first painted frame — plus crashed renderers, gone child/GPU processes, unresponsive pages, and uncaught errors in either process; any renderer error before the screen is drawn now shows as plain text, never blank. The log caught the failure live: the dev server listened only on IPv6 ::1 while the window sometimes dialled IPv4 127.0.0.1 for "localhost" — connection refused at random, hence intermittent blanks and the earlier dev-button error (CL-0020's stale-server diagnosis was incomplete). Proof: [::1]:5173 answered 200 while 127.0.0.1:5173 refused, same instant. Fix: the dev server now binds 127.0.0.1 explicitly, so nothing resolves "localhost" anywhere; the check screen also retries and fails fast instead of hanging. Windows Event Viewer checked: no GPU/child-process blocks or crashes today (only the known OQ-020 npm-install block); graphics layer not implicated, so no acceleration change made. Verified: ten scripted starts, pixel-checked screenshots, 10 of 10 Home screens; full check suite green twice, export check now ~6 s faster. | Alek reported blank windows persisting after CL-0021 and directed instrumentation-first diagnosis. | DOC-04, 10 |
| CL-0021 | 2026-09-02 | App | Start-up made reliable. Defect: the window loaded its screen exactly once with no wait, retry, or failure handling, so a moment where the dev server was not answering left a permanently blank window (seen by Alek several times). Fix in the main process: wait up to 15 s for the dev server to answer before the first load; retry any failed load (up to 40 times over ~20 s); if loading still fails, show a plain-text explanation in the window (what was tried, the error, what to do) — never blank; show the window after 15 s regardless as a last resort. Verified: five consecutive cold starts all reached the Home screen (driven live); the unreachable-server case shows the explanation page (tested against a dead port). Full check suite green. | Alek reported intermittent blank windows on npm run dev. | DOC-04, 10 |
| CL-0020 | 2026-09-02 | App (dev tools) | Alek's "Failed to fetch dynamically imported module" on the dev export button diagnosed by driving the real running app (create project, click both dev buttons — export succeeded: 300 frames, 5.3 s, hardware encoder). Root cause: a dev app left running from before the Phase 2 code update cannot load the newly added modules; a restart fixes it. The dev buttons now say exactly that in plain language instead of showing a TypeError. Also added a dev-only, env-gated hook (PAPERCUT_REMOTE_DEBUG_PORT) so Claude Code can drive the live app for testing; impossible in packaged builds. | Alek reported the error after trying the Phase 2 session report steps. | DOC-04, 10 |
| CL-0019 | 2026-09-02 | Docs / Decision | Phase 2 measurement session run on the dev laptop (Windows 11) and recorded in new DOC-12: 10 s at 1080p30 exports in 7.9 s (hardware) / 7.2 s (software forced) / 3.4 s (720p); 60 s of 1080p30 extrapolates to ≈ 43–47 s against the 180 s no-GPU target; drift 0.3 ms at every beep (9.6 ms at 0.0 s, AAC start-up); picture clean. Decision: OQ-019 closed, ADR-013 status now plain Accepted, DOC-08 A11 locks mp4-muxer 5.2.2 (MIT, with a note that npm deprecates it in favour of MPL-licensed Mediabunny — we stay), new row A19 for mp4box (BSD-3-Clause, check-only). DOC-12 added to the DOC-00 index. Windows 10 untested — no machine. | Phase 2 plan step 5–6: measure, then decide OQ-019 on the numbers. | DOC-00, 02, 04, 07, 08, 10, 12 |
| CL-0018 | 2026-09-02 | App | Phase 2 export prototype (OQ-019, ADR-013): ten-second test project generated in code under tests/fixtures (solid-colour PNGs and sine beeps written by hand, nothing downloaded; beeps at 0/2.5/5/7.5/9.5 s with a 3-frame white flash and a burned-in frame counter); export module independent of the editor (PixiJS off-screen frames → WebCodecs H.264 asking hardware first + AAC → mp4-muxer → file in the project folder, reporting which encoder answered); dev-only buttons on the opened-project view ("Load test content", "Export prototype"); Export check added to npm run check — it starts a hidden dev window, exports the test project at 1080p30, reads the .mp4 back (mp4box + WebCodecs decoders) and confirms duration, resolution, frame rate, frame count and per-beep audio/video drift ≤ 50 ms. New IPC (read/write project file, save document) is path-guarded and tested; new deps mp4-muxer 5.2.2 (MIT, ships) and mp4box 2.4.1 (BSD-3-Clause, check only); a production build was scanned to confirm no fixture/dev code ships. First result: check passes, hardware encoder, ≤ 9.6 ms drift. | Phase 2 plan approved by Alek; ADR-013 must be proven this week. | DOC-04, 10 |
| CL-0017 | 2026-09-02 | Verification | Alek manually tested Phase 1 on the dev laptop: create project (name with space, 9:16), folder layout on disk, back and reopen from Recent, non-project folder refused in plain English, duplicate/empty/illegal names refused, version number visible, window resizes, Recent persists across restart. All passed. Phase 1 verified by the owner; Phase 2 may begin. | DOC-11 §4 step 5 (Alek verifies before the next phase). | DOC-04, 10 |
| CL-0016 | 2026-09-02 | Docs | ADR-006 marked Accepted and OQ-006 closed (Alek approved the stack with the Phase 1 plan); DOC-03 now Active. Added OQ-020: Windows Smart App Control blocks unsigned native Node modules on the dev laptop — verify onnxruntime-node loads at the start of Phase 3. Manual sections M-1.2 (home screen) and M-9.3 (project folder) written. Tracker updated: Phase 1 usable, five checks live. | Close out Phase 1 per the approved plan. | DOC-00, 02, 03, 04, 05, 07, 10, README |
| CL-0015 | 2026-09-02 | App | Home screen: create a project (name, format 9:16/16:9/1:1, save location via folder picker), open an existing project, recent-projects list (max 10, self-cleaning). Retro dark UI, version number in the corner. After open, a placeholder view shows the loaded document until the editor exists. Verified live by driving the real app (playwright-core dev tool): end-to-end create produced a correct .papercut folder. | Phase 1 plan step: the app's entry screen. | DOC-04, 05, 10 |
| CL-0014 | 2026-09-02 | App | Network blocker: every outbound request is denied unless its origin is on an approved list, which is deliberately empty until the company server exists (Phase 10). In development only the Vite dev server is allowed. Check verifies the policy and that it is wired into startup. | ADR-015 "no unexpected network"; DOC-03 §6. | DOC-04, 10 |
| CL-0013 | 2026-09-02 | App | Provider layer (DOC-03 §7): interfaces for segmentation, tts, agent; fake TTS (deterministic silent WAV, generated in code) and fake agent (canned proposal); live-AI guard — constructing a live provider without PAPERCUT_LIVE_AI=1 fails. Checks: fakes are the default, guard env unset in tests, no paid-AI address anywhere in app code. | ADR-016 / DOC-09: development and checks must cost $0 in AI usage. | DOC-04, 10 |
| CL-0012 | 2026-09-02 | App | Project Document (DOC-03 §3) as strict TypeScript types; edit functions with structural sharing; undo/redo history (past/present/future); validation of loaded files; project folder create/save/load with atomic writes (DOC-03 §2). Checks: save/reopen field-for-field; 20 random edits + 20 undos return the exact starting object. | Phase 1 plan step: the document is the heart of the app. | DOC-04, 10 |
| CL-0011 | 2026-09-02 | Tooling | Scaffold: Electron 44, TypeScript strict, React 19, Vite 7 + electron-vite, Vitest, electron-builder; PixiJS installed for Phase 2. License checker enforcing the DOC-08 §7 allow-list with registered build-time exceptions (A17 caniuse-lite, A18 truncate-utf8-bytes). One command, npm run check, runs typecheck + tests + license check. | Phase 1 plan step: tooling before features. | DOC-04, 08, 10 |
| CL-0010 | 2026-09-02 | Repo | git repository initialised on main in the project folder; existing docs, CLAUDE.md, README, .gitignore kept; .gitattributes added; remote origin set to GitHub and first push made. | Phase 1 plan step: version history and off-site backup from day one. | DOC-04, 10 |
| CL-0009 | 2026-09-02 | Docs | Repository created at https://github.com/aleknowak1/papercut.git; local folder fixed. Recorded in CLAUDE.md, README, DOC-11, and the Phase 1 prompt (DOC-11 Appendix B; no prompt files kept in the repo). | Alek created the GitHub repository and authenticated the laptop. | CLAUDE.md, README, DOC-04, 11, DOC-11 Appendix B |
| CL-0008 | 2026-09-02 | Docs | Added DOC-11 Development Workflow: Claude Code on the Windows laptop with a git/GitHub repo as source of truth, CLAUDE.md standing orders (Appendix A), session rhythm, roles, sub-agent policy, Phase 1 kickoff prompt (Appendix B). | Alek asked how to run Phase 1 with Fable and whether to use prompts and agents. | DOC-00, 04, 11 |
| CL-0007 | 2026-09-02 | Docs | Added DOC-10 Project Tracker (phase status, feature checklist, check-suite status, waiting-on-Alek list). Recorded model-usage plan per phase (Fable for foundations, Opus for contained feature work). Tracker update added to change discipline in DOC-00. | Alek asked for a single place to see progress. | DOC-00, 04, 10 |
| CL-0006 | 2026-09-02 | Docs | Added ADR-015 (automated checks) closing OQ-010, ADR-016 (AI spend discipline), and DOC-09 AI Spend Policy. Glossary terms added. | Alek approved the testing approach and directed that testing and development must not spend AI tokens; AI usage must be treated carefully throughout. | DOC-00, 02, 04, 06, 07, 09 |
| CL-0005 | 2026-09-02 | Docs | ADR-014 (merchant of record) accepted. OQ-014 assigned: Claude drafts ToS/privacy/acceptable-use at the end of the build order, Alek approves. | Alek confirmed. | DOC-02, 04, 07 |
| CL-0004 | 2026-09-02 | Docs | Windows-only v1 (ADR-012). Export via Windows' built-in encoders through WebCodecs, no FFmpeg (ADR-013). Payments via merchant of record, proposed (ADR-014). Optional "AI-generated" export label and sign-up acceptable-use agreement added to DOC-01. Closed OQ-001, 003, 015, 016, 017 with lawyer-free resolutions; OQ-014 given a template path; added OQ-018, 019. DOC-08 now has no items requiring legal review. | Alek cannot engage a lawyer and has no Mac to test on; every "legal review" item was replaced with a well-trodden path that needs none. | DOC-01, 02, 03, 04, 07, 08 |
| CL-0003 | 2026-09-02 | Docs | Added DOC-08 Licensing Register: verified licenses of all application and server components against their license texts; recorded FFmpeg LGPL checklist, OpenAI obligations (TTS disclosure, output ownership, likeness), H.264 patent position, HEIC decision. Closed OQ-007 (min specs). Narrowed OQ-003. Added OQ-015 to OQ-017. | Alek requested a licensing document and confirmation of the full stack before reviewing the testing approach. | DOC-00, 04, 07, 08 |
| CL-0002 | 2026-09-02 | Docs | Revised v1 scope: company server, accounts, subscription, cloud TTS (OpenAI gpt-4o-mini-tts), and the AI agent (OpenAI GPT-4o-mini class) move into v1.0. Background removal fixed as BiRefNet in two bundled sizes; ISNet and Kokoro dropped. ADR-003 and ADR-004 superseded by ADR-008 to ADR-011. OQ-002 closed; OQ-011 to OQ-014 added. | Alek chose a simpler, connected v1 at higher running cost over an offline v1. Cloud TTS made the server unavoidable, which made the agent a natural v1 inclusion. | DOC-01, 02, 03, 05, 06, 07 |
| CL-0001 | 2026-09-02 | Docs | Created the foundation document set (DOC-00 through DOC-07). Recorded decisions ADR-001 to ADR-007. Verified licenses of candidate bundled models. | Establish a solid foundation before development begins. | DOC-00, 01, 02, 03, 04, 05, 06, 07 |
